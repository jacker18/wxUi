function setWH() {
	// if(dom.navigator){

	// }
	window.moveTo(0, 0); //移动窗口   
	window.resizeTo(800, 600); //改变大小   
	// window.onresize = new Function("window.resizeTo(800,   600);")
	// window.onload = new Function("window.moveTo(0, 0); window.resizeTo(800,   600);")
	console.log("设置成功")
}